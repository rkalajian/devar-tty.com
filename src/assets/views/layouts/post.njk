--- 
layout: base.njk 
---
<div id="post">
    <h2>{{ title }}</h2>
    Posted in <strong>{{ category }}</strong> on <em>{{ date | dateReadable }}</em>
    {% if featuredImage %}<img src="{{ featuredImage | url }}" alt="{{ featuredImage }}"></img> {% endif %}
    <p>{{ content | safe }}</p>
</div>